# LET foo = bar * 3 + 2
# IF foo > 0 THEN
#     IF 10 * 10 < 100 THEN
#         PRINT bar
#     ENDIF
# ENDIF

# LABEL test
# LABEL main
# LET index = 2

# PRINT "hello, world"
# PRINT index
# GOTO main

PRINT "How many fibonacci numbers do you want?"
INPUT nums
PRINT ""

LET a = 0
LET b = 1
WHILE nums > 0 REPEAT
    PRINT a
    LET c = a + b
    LET a = b
    LET b = c
    LET nums = nums - 1
ENDWHILE
